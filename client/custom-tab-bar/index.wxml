<!--custom-tab-bar/index.wxml - 极简 TabBar v3.0-->
<view class="tabbar">
  <view
    wx:for="{{list}}" wx:key="index"
    class="tab {{selected === index ? 'tab-on' : ''}}"
    data-index="{{index}}" bindtap="switchTab"
  >
    <text class="tab-text">{{item.text}}</text>
  </view>
</view>
