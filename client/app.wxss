/* app.wxss - HH小程序全局样式 v3.0 - 浅色系 */

/* ===== 设计变量 ===== */
page {
  /* 背景层次：最浅→卡片→浮起 */
  --color-bg: #f5f6fa;
  --color-bg-subtle: #eef0f6;
  --color-bg-card: #ffffff;
  --color-bg-elevated: #ffffff;
  --color-bg-glass: rgba(255, 255, 255, 0.78);

  /* 主题色 */
  --color-primary: #7c6ff7;
  --color-primary-light: #9d93fa;
  --color-primary-dark: #5b4fd4;
  --color-accent: #4ecdc4;
  --color-accent-warm: #ff6b6b;

  /* 文字层次 */
  --color-text: #1a1a2e;
  --color-text-secondary: #6b6b82;
  --color-text-muted: #a0a0b4;

  /* 边框与分割 */
  --color-border: rgba(0,0,0,0.06);
  --color-border-light: rgba(0,0,0,0.09);

  /* 语义色 */
  --color-success: #4ecdc4;
  --color-warning: #f5a623;
  --color-danger: #ff6b6b;

  /* 字号 */
  --font-size-xs: 20rpx;
  --font-size-sm: 24rpx;
  --font-size-base: 28rpx;
  --font-size-md: 32rpx;
  --font-size-lg: 36rpx;
  --font-size-xl: 44rpx;
  --font-size-xxl: 56rpx;

  /* 圆角 */
  --radius-sm: 12rpx;
  --radius-md: 20rpx;
  --radius-lg: 28rpx;
  --radius-xl: 36rpx;

  /* 间距 */
  --spacing-xs: 8rpx;
  --spacing-sm: 16rpx;
  --spacing-md: 24rpx;
  --spacing-lg: 32rpx;
  --spacing-xl: 48rpx;

  /* 全局 */
  background-color: var(--color-bg);
  color: var(--color-text);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  font-size: var(--font-size-base);
  line-height: 1.6;
}

/* ===== 全局重置 ===== */
view, text, image, scroll-view { box-sizing: border-box; }

/* ===== 通用类 ===== */
.container {
  padding: var(--spacing-md);
  padding-bottom: calc(var(--spacing-xl) + env(safe-area-inset-bottom));
}

.flex-row { display: flex; flex-direction: row; align-items: center; }
.flex-col { display: flex; flex-direction: column; }
.flex-center { display: flex; justify-content: center; align-items: center; }
.flex-between { display: flex; justify-content: space-between; align-items: center; }
.flex-1 { flex: 1; }

.text-primary { color: var(--color-primary); }
.text-secondary { color: var(--color-text-secondary); }
.text-muted { color: var(--color-text-muted); }
.text-accent { color: var(--color-accent); }
.text-warning { color: var(--color-warning); }
.text-danger { color: var(--color-danger); }
.text-sm { font-size: var(--font-size-sm); }
.text-base { font-size: var(--font-size-base); }
.text-md { font-size: var(--font-size-md); }
.text-lg { font-size: var(--font-size-lg); }
.text-xl { font-size: var(--font-size-xl); }

/* ===== 卡片样式 ===== */
.card {
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  border: 1rpx solid var(--color-border);
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}

.card-elevated {
  background: var(--color-bg-elevated);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  border: 1rpx solid var(--color-border-light);
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.06);
}

.card-glass {
  background: var(--color-bg-glass);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  border: 1rpx solid var(--color-border-light);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}

/* ===== 按钮样式 ===== */
.btn-primary {
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
  color: white;
  border-radius: var(--radius-xl);
  padding: 24rpx 48rpx;
  font-size: var(--font-size-md);
  font-weight: 600;
  text-align: center;
  border: none;
  box-shadow: 0 6rpx 24rpx rgba(124, 111, 247, 0.25);
}
.btn-primary::after { border: none; }

.btn-outline {
  background: transparent;
  color: var(--color-primary);
  border: 2rpx solid var(--color-primary);
  border-radius: var(--radius-xl);
  padding: 22rpx 46rpx;
  font-size: var(--font-size-md);
  font-weight: 600;
  text-align: center;
}
.btn-outline::after { border: none; }

.btn-ghost {
  background: var(--color-bg-subtle);
  color: var(--color-text);
  border-radius: var(--radius-xl);
  padding: 22rpx 46rpx;
  font-size: var(--font-size-md);
  font-weight: 500;
  text-align: center;
  border: 1rpx solid var(--color-border);
}
.btn-ghost::after { border: none; }

/* ===== 标签样式 ===== */
.tag {
  display: inline-flex;
  align-items: center;
  padding: 6rpx 16rpx;
  border-radius: var(--radius-sm);
  font-size: var(--font-size-xs);
  background: var(--color-bg-subtle);
  color: var(--color-text-secondary);
  border: 1rpx solid var(--color-border);
}
.tag-active {
  background: var(--color-primary);
  color: white;
  border-color: var(--color-primary);
}

/* ===== 分割线 ===== */
.divider { height: 1rpx; background: var(--color-border-light); margin: var(--spacing-md) 0; }

/* ===== 滚动区域 ===== */
.scroll-h { white-space: nowrap; }

/* ===== 加载状态 ===== */
.loading-container { display: flex; justify-content: center; align-items: center; padding: 80rpx 0; }

/* ===== 空状态 ===== */
.empty-state { display: flex; flex-direction: column; align-items: center; padding: 120rpx 60rpx; }
.empty-state .empty-icon { font-size: 80rpx; margin-bottom: var(--spacing-md); }
.empty-state .empty-text { font-size: var(--font-size-base); color: var(--color-text-secondary); }

/* ===== 安全区域底部 ===== */
.safe-bottom { padding-bottom: calc(var(--spacing-lg) + env(safe-area-inset-bottom)); }
